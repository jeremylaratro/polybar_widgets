# Brightness

Display and control screen brightness with scroll wheel.

## Features

- Shows current brightness percentage
- Scroll up: increase brightness
- Scroll down: decrease brightness

## Requirements

- `brightnessctl`

## Configuration

Set custom step size via environment variable:
```bash
export BRIGHTNESS_STEP=10  # default is 5
```
