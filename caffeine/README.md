# Caffeine

Prevent screen from turning off (like a coffee keeps you awake).

## Features

- Click to toggle caffeine mode
- Shows ON/OFF status
- Saves and restores previous timeout settings

## Requirements

- `xset` (X11)

## How it works

When enabled, disables DPMS and screensaver. When disabled, restores previous settings.
