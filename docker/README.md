# Docker

Show running/total Docker container count.

## Features

- Shows running/total containers (e.g., "3/5")
- Shows "OFF" if Docker daemon not running
- Shows "N/A" if Docker not installed

## Requirements

- `docker`
